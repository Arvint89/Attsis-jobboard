Follow-up to #30. The live map colours companies from the server's base-model scoring, not the loaded CV, and the Home marker is unlabelled.

Fix (finished code, parked in `stash@{0}`): labelled "Home" marker, company status recomputed from the CV-rescored pool, unresolved companies drawn smaller and excluded from zoom bounds.
