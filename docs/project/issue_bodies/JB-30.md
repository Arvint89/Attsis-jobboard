The daily sweep sees ~233 embedded + ~30 PCB results on Communitech; the live board keeps 1 Communitech job. `gather()` reports no error, so jobs are either not fetched or silently excluded by `jobfilter.classify`.

- JB-30a: per-source funnel stats in jobs.json (measure first) - `docs/briefs/JB-30a-source-funnel-stats.md`
- JB-30b: one failed Getro query must not wipe the others; paginate past page 0 - `docs/briefs/JB-30b-getro-resilience-and-pagination.md`
