__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# secrets — never commit
.env
.env.local
.env.*.local

# aider working files
.aider*
!.aider.conf.yml
!.aiderignore

# generated by build_board.py (CI + the job-sweep Action rebuild them every run) - JB-37
site/data/jobs.json
site/board_standalone.html

# local-only scratch folder (purpose unknown, intentionally not tracked)
memory/
site/data/history.json
site/data/resolve.json
